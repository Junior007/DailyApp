<Window x:Class="daily.UI.Views.DailyWorkView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:daily.UI.Views"
        xmlns:vm ="clr-namespace:daily.UI.ViewsModel.Infrastructure"
        vm:Locator.IsAutomaticLocator="True"
        mc:Ignorable="d"
        Title="Daily Tasks" Height="450" Width="800">
    <Grid  DataContext="{Binding DailyWork}">
        <Grid.RowDefinitions>
            <RowDefinition Height="34"></RowDefinition>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <Button Content="Add" Command="{Binding AddCommand}">
            </Button>
            <Button Content="Delete" Command="{Binding DeleteCommand}"></Button>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition  Width="150"></ColumnDefinition>
                <ColumnDefinition  Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="100"></RowDefinition>
                <RowDefinition Height="100"></RowDefinition>
            </Grid.RowDefinitions>
            <TextBlock Text="{Binding Title}" Grid.Column="0"  Grid.Row="0" Margin="1"></TextBlock>
            <TextBox  Grid.Column="1" Grid.Row="0" 
                Text="{Binding Description}" 
                Margin="0,0,20,0"
                AcceptsReturn="True"
                FontSize="14"/>
            <ListView 
                ItemsSource="{Binding SubTasks}"
                Grid.Column="1" Grid.Row="1" 
                >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition  Width="150"></ColumnDefinition>
                                <ColumnDefinition  Width="*"></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Title}" Grid.Column="0" Margin="1"></TextBlock>
                            <TextBox    Grid.Column="1"
                                        Text="{Binding Description}" 
                                        Margin="0,0,20,0"
                                        AcceptsReturn="True"
                                        FontSize="14"/>
                        </Grid>

                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Window>
