<UserControl x:Class="daily.UI.Views.Controls.DailyTaskDetail"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm ="clr-namespace:daily.UI.Infrastructure"
             vm:Locator.IsAutomaticLocator="True"

             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="100">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions> 
        
        <TextBox Grid.Column="0" Grid.Row="0" Text="{Binding Title, Mode=TwoWay}" ></TextBox>
        <TextBox Grid.Column="1" Grid.Row="0" Text="{Binding Description, Mode=TwoWay}" ></TextBox>
        
  
    </Grid>
</UserControl>
